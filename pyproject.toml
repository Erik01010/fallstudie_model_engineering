[project]
name = "fallstudie"
version = "1.0"
description = "uni: fallstudie Model Engineering"
authors = [
    {name = "Erik R"}
]
license = {text = "-"}
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
